pynput>=1.7.0,<2.0.0
pygame>=2.0.0,<3.0.0
numpy>=1.20.0,<2.0.0
psutil>=5.8.0,<6.0.0

# Optional dependencies for better compatibility
# These will be installed if available but won't fail if missing
# playsound>=1.2.2  # Alternative audio backend
# pyaudio>=0.2.11   # Alternative audio system
